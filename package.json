{
  "name": "firebase-increment-id",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && npx firebase emulators:start --only functions,firestore --project firebase-increment-id",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "@firebase/logger": "^0.4.2",
    "express": "^4.19.2",
    "firebase-admin": "^12.2.0",
    "firebase-functions": "^5.0.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.12",
    "firebase-tools": "^13.14.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.3",
    "typescript": "^5.5.4"
  },
  "private": true
}
